<?php

/**
 * Implements hook_theme().
 */
function time_ticker_theme($existing, $type, $theme, $path) {
    return [
      'time_ticker_block' => [
        'variables' => ['current_time' => NULL],
      ],
    ];
  }